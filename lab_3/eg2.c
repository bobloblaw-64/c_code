#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(int argc, char *argv[])
{
    char in[10];
    printf("Give the input:\n");
    scanf("%s", in);
    printf("The input is:%s\n",in);
}